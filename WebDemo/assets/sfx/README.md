# Sound Effects

Place OGG audio files here:

- `appear.ogg` - Played when museum card appears (subtle UI bleep)
- `reply.ogg` - Played when AI reply appears (gentle chime)

For now, the app will gracefully fail if these files are not present.

